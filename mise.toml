# Toolchain for lazymux. Tasks are uv/Python scripts in `.mise/tasks/`;
# run `mise tasks` to list them, `mise run <task> --help` for a script's options.

[tools]
go = "1.26"
lefthook = "2.1"
uv = "0.12"
